import { StyleSheet } from 'react-native'

export const screenStyles = StyleSheet.create({
    fullyCentered: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
        width: '100%',
        paddingBottom: 20,
    },
    verticallyCentered: {
        flex: 1,
        justifyContent: 'center',
    },
})
